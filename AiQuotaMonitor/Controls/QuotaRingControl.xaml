<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AiQuotaMonitor.Controls.QuotaRingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!-- 56pt 进度环（参考 cc-bar Popover 服务 5h 环）：轨道 + 进度弧 + 中央百分比/标签 -->
    <Grid x:Name="Root" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Path x:Name="TrackPath" Stroke="{ThemeResource CardBorderBrush}" StrokeThickness="5.5"/>
        <Path x:Name="ValuePath" StrokeThickness="5.5" StrokeStartLineCap="Round" StrokeEndLineCap="Round"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="ValueText" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
            <TextBlock x:Name="LabelText" FontSize="8" Opacity="0.55" HorizontalAlignment="Center" CharacterSpacing="20"/>
        </StackPanel>
    </Grid>
</UserControl>
