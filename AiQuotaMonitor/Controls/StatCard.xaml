<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AiQuotaMonitor.Controls.StatCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!-- KPI 卡片（参考 cc-bar Statistics KPI row） -->
    <Border Background="{ThemeResource CardBackgroundBrush}"
            BorderBrush="{ThemeResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="16"
            Padding="16">
        <StackPanel Spacing="6">
            <TextBlock x:Name="LabelHost" FontSize="12" Foreground="{ThemeResource SubtleTextBrush}"/>
            <TextBlock x:Name="ValueHost" FontSize="24" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
            <TextBlock x:Name="SubLabelHost" FontSize="11" Foreground="{ThemeResource SubtleTextBrush}"/>
        </StackPanel>
    </Border>
</UserControl>
